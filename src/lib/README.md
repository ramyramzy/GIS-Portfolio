# Library and shared helpers

This folder is reserved for shared utilities, constants, and future helper modules.
Keep app-specific components in the components folder and page-specific logic in app.
